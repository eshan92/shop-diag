=== SCHEMA ===
lots
CREATE TABLE lots (id INTEGER PRIMARY KEY AUTOINCREMENT, buy REAL NOT NULL, suffix TEXT NOT NULL, sell REAL NOT NULL, pieces INTEGER NOT NULL, min_pieces INTEGER DEFAULT 50, note TEXT DEFAULT '', date TEXT NOT NULL)

sqlite_sequence
CREATE TABLE sqlite_sequence(name,seq)

products
CREATE TABLE products (id INTEGER PRIMARY KEY AUTOINCREMENT, name TEXT NOT NULL, unit TEXT NOT NULL, buy_price REAL NOT NULL, sell_price REAL NOT NULL, stock INTEGER NOT NULL, min_stock INTEGER DEFAULT 50)

sales
CREATE TABLE sales (id INTEGER PRIMARY KEY AUTOINCREMENT, date TEXT NOT NULL, time TEXT NOT NULL, total REAL NOT NULL, profit REAL NOT NULL, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP)

sale_items
CREATE TABLE sale_items (id INTEGER PRIMARY KEY AUTOINCREMENT, sale_id INTEGER NOT NULL, name TEXT NOT NULL, type TEXT NOT NULL, ref_id INTEGER, buy_price REAL NOT NULL, sell_price REAL NOT NULL, qty INTEGER NOT NULL, subtotal REAL NOT NULL, profit REAL NOT NULL)

stock_in
CREATE TABLE stock_in (id INTEGER PRIMARY KEY AUTOINCREMENT, date TEXT NOT NULL, product_id INTEGER, product_name TEXT NOT NULL, qty INTEGER NOT NULL, cost_per_unit REAL NOT NULL, total_cost REAL NOT NULL, supplier TEXT DEFAULT '')

expenses
CREATE TABLE expenses (id INTEGER PRIMARY KEY AUTOINCREMENT, date TEXT NOT NULL, category TEXT NOT NULL, amount REAL NOT NULL, description TEXT DEFAULT '')

settings
CREATE TABLE settings (key TEXT PRIMARY KEY, value TEXT NOT NULL)

bot_upnl_history
CREATE TABLE bot_upnl_history (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    bot_name TEXT NOT NULL,
    timestamp TEXT NOT NULL,
    upnl REAL DEFAULT 0,
    realized REAL DEFAULT 0,
    true_pnl REAL DEFAULT 0,
    available REAL DEFAULT 0,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
)

market_behavior
CREATE TABLE market_behavior (
        id INTEGER PRIMARY KEY AUTOINCREMENT,
        coin TEXT NOT NULL,
        timestamp TEXT NOT NULL,
        unix_ts INTEGER NOT NULL,
        price REAL NOT NULL,
        volume REAL DEFAULT 0,
        rsi REAL DEFAULT 0,
        price_1h_ago REAL DEFAULT 0,
        price_4h_ago REAL DEFAULT 0,
        high_4h REAL DEFAULT 0,
        low_4h REAL DEFAULT 0,
        range_pct REAL DEFAULT 0,
        position_in_range REAL DEFAULT 0,
        direction TEXT DEFAULT 'FLAT',
        sideways_score REAL DEFAULT 0,
        created_at DATETIME DEFAULT CURRENT_TIMESTAMP
    )

=== CASHIER UI ===
227:#cashier-app{display:none;min-height:100vh;background:var(--bg);}
240:/* cashier lot cards */
257:/* cashier other btns */
265:/* cashier order */
406:      <div class="role-btn" id="role-cashier" onclick="selRole('cashier')">
457:        <div class="sc purple"><div class="si">🛒</div><div class="sl">Today Sales</div><div class="sv" id="s4">0</div><div class="su">transactions</div></div>
460:        <div class="card"><div class="ct">Today Sales</div><table><thead><tr><th>Time</th><th>Items</th><th>Amount</th><th>Profit</th></tr></thead><tbody id="dsales"></tbody></table></div>
646:            <button class="btn bs" onclick="chgPass('cashier')">Update Cashier</button>
686:<div id="cashier-app">
693:      <button class="ctab active" onclick="cTab('sale',this)">🛒 New Sale</button>
694:      <button class="ctab" onclick="cTab('today',this)">📋 Today</button>
757:  document.getElementById('role-cashier').className='role-btn'+(r==='cashier'?' selected':'');
762:  else{document.getElementById('cashier-app').style.display='block';rCashier();}
781:  document.getElementById('cashier-app').style.display='none';
1205:  var pw=DB_pw||{admin:'admin123',cashier:'cashier123'};
1220:    api('POST','/api/change-password',{role:'cashier',admin_pass:ap,new_password:cnw}).then(function(d){
1285:function cTab(tab,el){
=== TG SEND ===
28:        url = "https://api.telegram.org/bot" + TOKEN + "/sendMessage"
